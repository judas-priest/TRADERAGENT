# ⚠️ НАСТРОЙКА BYBIT PRODUCTION - РЕАЛЬНАЯ ТОРГОВЛЯ

## 🚨 КРИТИЧЕСКИЕ ПРЕДУПРЕЖДЕНИЯ

**ПРОЧИТАЙТЕ ВНИМАТЕЛЬНО ПЕРЕД ПРОДОЛЖЕНИЕМ:**

1. ⚠️ **ВЫ МОЖЕТЕ ПОТЕРЯТЬ ВСЕ СРЕДСТВА**
2. ⚠️ **НАЧИНАЙТЕ С МАЛЫХ СУММ** (максимум 1-5% от капитала)
3. ⚠️ **ПРОТЕСТИРУЙТЕ НА TESTNET МИНИМУМ 2 НЕДЕЛИ**
4. ⚠️ **НАСТРОЙТЕ STOP-LOSS И RISK MANAGEMENT**
5. ⚠️ **НЕ ДАВАЙТЕ ПРАВА WITHDRAW ДЛЯ API КЛЮЧЕЙ**

---

## Информация о серверах

### Продакшн сервер (где работает бот):
- **IP:** `185.233.200.13`
- **Пользователь:** `ai-agent`
- **Назначение:** Production bot

### Текущий сервер (тестовый):
- **IP:** `173.249.2.184`
- **Пользователь:** `hive`
- **Назначение:** Development/Testing

---

## ШАГ 1: Получение ByBit MAINNET API ключей

### 1.1 Создайте API ключ на ByBit MAINNET

⚠️ **ВАЖНО: НЕ testnet, а MAINNET!**

```
URL: https://www.bybit.com/
```

1. Войдите в свой **реальный** аккаунт ByBit
2. Перейдите: **Account & Security** → **API Management**
3. Нажмите **Create New Key**

### 1.2 Настройте разрешения API ключа

**ОБЯЗАТЕЛЬНЫЕ разрешения:**
- ✅ **Read** - чтение данных
- ✅ **Spot Trading** - торговля на споте

**ЗАПРЕЩЕННЫЕ разрешения:**
- ❌ **Withdraw** - НИКОГДА НЕ ВКЛЮЧАЙТЕ!
- ❌ **Transfer** - не нужно для торговли
- ❌ **Sub-account** - не нужно

### 1.3 Настройте IP Whitelist (РЕКОМЕНДУЕТСЯ)

Добавьте IP адрес продакшн сервера в белый список:
```
185.233.200.13
```

Это защитит ваш API ключ от использования с других IP адресов.

### 1.4 Сохраните ключи

⚠️ **API Secret показывается только один раз!**

Сохраните в безопасном месте:
- **API Key:** `ваш_api_key`
- **API Secret:** `ваш_api_secret`

---

## ШАГ 2: Подключение к продакшн серверу

### 2.1 Подключитесь к серверу

```bash
ssh ai-agent@185.233.200.13
```

### 2.2 Перейдите в директорию проекта

```bash
cd /path/to/TRADERAGENT
# Или найдите:
find /home -name "TRADERAGENT" -type d 2>/dev/null
```

---

## ШАГ 3: Настройка окружения

### 3.1 Проверьте .env файл

```bash
cat .env | grep -E "(DATABASE_URL|ENCRYPTION_KEY)"
```

**Если .env нет, создайте:**

```bash
cp .env.example .env
nano .env
```

### 3.2 Убедитесь, что ENCRYPTION_KEY установлен

```bash
# Проверить
grep ENCRYPTION_KEY .env

# Если нет, сгенерировать
python3 -c "import os, base64; print('ENCRYPTION_KEY=' + base64.b64encode(os.urandom(32)).decode())"
```

**Добавьте в .env:**
```bash
ENCRYPTION_KEY=ваш_сгенерированный_ключ_base64
```

⚠️ **ВАЖНО:** Сохраните этот ключ! Без него вы не сможете расшифровать credentials!

---

## ШАГ 4: Добавление ByBit PRODUCTION credentials

### 4.1 Убедитесь, что PostgreSQL запущена

```bash
# Проверить
docker-compose ps | grep postgres
# или
sudo systemctl status postgresql
```

**Если не запущена:**
```bash
# Docker:
docker-compose up -d postgres
sleep 5

# Или локальный PostgreSQL:
sudo systemctl start postgresql
```

### 4.2 Примените миграции (если еще не применены)

```bash
# Проверить текущую версию
alembic current

# Применить миграции
alembic upgrade head
```

### 4.3 Запустите скрипт добавления credentials

```bash
python add_bybit_credentials.py
```

**⚠️ ВАЖНО: Вводите MAINNET credentials!**

```
Name: bybit_production
API Key: ваш_mainnet_api_key
API Secret: ваш_mainnet_api_secret
Choice: 2  ⚠️ ВЫБЕРИТЕ 2 (MAINNET, НЕ TESTNET!)
```

При запросе подтверждения:
```
Type 'YES' to confirm: YES
```

### 4.4 Проверьте, что credentials добавлены

```bash
# Проверить в базе данных
psql -U traderagent -d traderagent -c "SELECT name, exchange_id, is_sandbox FROM exchange_credentials;"
```

Должно показать:
```
name              | exchange_id | is_sandbox
------------------+-------------+-----------
bybit_production  | bybit       | f
```

⚠️ **is_sandbox должен быть FALSE (f)!**

---

## ШАГ 5: Тестирование подключения

### 5.1 Запустите тест подключения

```bash
python test_bybit_connection.py --credentials bybit_production
```

**Ожидаемый результат:**
```
✅ All Tests Passed!
Your ByBit API connection is working correctly!
```

**Проверьте:**
- ✅ Подключение к API успешно
- ✅ Баланс загружается
- ✅ Тикеры работают
- ✅ Минимальные размеры ордеров корректны

---

## ШАГ 6: Настройка бота для PRODUCTION

### 6.1 Создайте продакшн конфигурацию

```bash
# Скопируйте пример
cp configs/bybit_example.yaml configs/production_bybit.yaml

# Отредактируйте
nano configs/production_bybit.yaml
```

### 6.2 КРИТИЧЕСКИЕ параметры для PRODUCTION

⚠️ **ОБЯЗАТЕЛЬНО измените:**

```yaml
bots:
  - name: bybit_prod_bot
    symbol: BTC/USDT  # Или другая пара
    strategy: grid     # grid, dca, или hybrid

    exchange:
      exchange_id: bybit
      credentials_name: bybit_production  # ⚠️ Имя из Step 4.3
      sandbox: false                      # ⚠️ FALSE для production!
      rate_limit: true

    # Начните с КОНСЕРВАТИВНЫХ параметров!
    grid:
      upper_price: "45000"
      lower_price: "40000"
      grid_levels: 20              # Больше уровней = меньше риск
      amount_per_grid: "10"        # ⚠️ НАЧНИТЕ С МАЛЫХ СУММ!
      profit_per_grid: "0.02"      # 2% прибыли

    risk_management:
      max_position_size: "500"     # ⚠️ ОГРАНИЧЬТЕ МАКСИМУМ!
      stop_loss_percentage: "0.15" # 15% stop-loss
      max_daily_loss: "50"         # Максимум $50 потерь в день
      min_order_size: "10"

    notifications:
      enabled: true                # Включите уведомления!
      notify_on_trade: true
      notify_on_error: true

    dry_run: true  # ⚠️ СНАЧАЛА ТЕСТИРУЙТЕ С dry_run: true!
    auto_start: false
```

### 6.3 Первый запуск - DRY RUN режим

⚠️ **ОБЯЗАТЕЛЬНО: Сначала запустите с dry_run: true**

```bash
# Запустить в dry_run режиме (БЕЗ реальных ордеров)
python -m bot.main --config configs/production_bybit.yaml
```

**Проверьте в логах:**
- ✅ Подключение к ByBit успешно
- ✅ Цены загружаются
- ✅ Бот принимает решения
- ✅ "DRY RUN" отображается в логах
- ✅ Ордера НЕ размещаются реально

**Тестируйте минимум 24-48 часов в dry_run режиме!**

---

## ШАГ 7: Переход к реальной торговле

### ⚠️ ЧЕКЛИСТ ПЕРЕД ЗАПУСКОМ:

- [ ] Протестировано 2+ недели на testnet
- [ ] Протестировано 24+ часов в production dry_run
- [ ] Все тесты проходят успешно
- [ ] Stop-loss настроен и работает
- [ ] Risk management ограничения установлены
- [ ] Telegram уведомления работают
- [ ] Понимание рисков и готовность к потерям
- [ ] **Начинаете с МАЛЫХ сумм** (1-5% капитала)
- [ ] Мониторинг настроен
- [ ] Backup базы данных настроен

### 7.1 Измените конфигурацию на реальную торговлю

```bash
nano configs/production_bybit.yaml
```

**Измените:**
```yaml
dry_run: false  # ⚠️ FALSE = РЕАЛЬНЫЕ ОРДЕРА!
```

### 7.2 Запустите бота

```bash
# В фоне с логами
nohup python -m bot.main --config configs/production_bybit.yaml > bot_production.log 2>&1 &

# Или в screen/tmux
screen -S traderagent
python -m bot.main --config configs/production_bybit.yaml
# Ctrl+A, D для отсоединения
```

### 7.3 Мониторинг

```bash
# Просмотр логов в реальном времени
tail -f bot_production.log

# Через Telegram
/status
/balance
/trades
```

---

## ШАГ 8: Безопасность и мониторинг

### 8.1 Настройте алерты

В конфигурации Telegram бота должны быть включены уведомления:
- Каждая сделка
- Ошибки API
- Достижение stop-loss
- Высокая частота ошибок

### 8.2 Регулярные проверки

**Ежедневно:**
- Проверяйте баланс
- Проверяйте открытые ордера
- Проверяйте P&L (прибыль/убыток)
- Проверяйте логи на ошибки

**Еженедельно:**
- Делайте backup базы данных
- Анализируйте производительность
- Корректируйте параметры если нужно

### 8.3 Backup базы данных

```bash
# Создать backup
pg_dump -U traderagent traderagent > backup_$(date +%Y%m%d).sql

# Настроить автоматический backup (cron)
crontab -e
# Добавить:
0 2 * * * pg_dump -U traderagent traderagent > /backups/traderagent_$(date +\%Y\%m\%d).sql
```

---

## Примеры консервативных стратегий

### Консервативная Grid стратегия (для начинающих)

```yaml
grid:
  upper_price: "45000"
  lower_price: "40000"
  grid_levels: 30              # Много уровней
  amount_per_grid: "10"        # Малые суммы
  profit_per_grid: "0.02"      # 2% прибыли

risk_management:
  max_position_size: "300"
  stop_loss_percentage: "0.10"  # 10% stop-loss
  max_daily_loss: "30"
```

### Безопасная DCA стратегия

```yaml
dca:
  trigger_percentage: "0.05"      # 5% падение
  amount_per_step: "15"
  max_steps: 3                    # Только 3 шага
  take_profit_percentage: "0.10"  # 10% прибыль

risk_management:
  max_position_size: "200"
  stop_loss_percentage: "0.15"
```

---

## Troubleshooting

### Проблема: "Authentication failed"
**Решение:**
- Проверьте, что используете MAINNET ключи (не testnet)
- Убедитесь, что IP сервера в whitelist на ByBit
- Проверьте разрешения API ключа

### Проблема: "Insufficient balance"
**Решение:**
- Пополните баланс на ByBit
- Уменьшите amount_per_grid/amount_per_step
- Проверьте минимальные размеры ордеров

### Проблема: "Order rejected"
**Решение:**
- Проверьте минимальный размер ордера для пары
- Убедитесь, что цена в допустимом диапазоне
- Проверьте баланс

---

## 🚨 КРИТИЧЕСКИЕ ПРАВИЛА

1. **НИКОГДА** не давайте права Withdraw для API ключей
2. **ВСЕГДА** начинайте с малых сумм (1-5% капитала)
3. **ОБЯЗАТЕЛЬНО** настройте stop-loss
4. **РЕГУЛЯРНО** проверяйте бота (минимум ежедневно)
5. **НЕ ПАНИКУЙТЕ** при просадках - бот работает по стратегии
6. **ДЕЛАЙТЕ BACKUP** базы данных регулярно
7. **МОНИТОРЬТЕ** логи и уведомления
8. **НЕ ИЗМЕНЯЙТЕ** параметры во время активной торговли
9. **ТЕСТИРУЙТЕ** любые изменения сначала на testnet
10. **ПОМНИТЕ**: торговля криптовалютой - высокий риск!

---

## Контакты и поддержка

- GitHub Issues: https://github.com/alekseymavai/TRADERAGENT/issues
- Документация: [README.md](README.md)

---

## ⚠️ DISCLAIMER

**ВЫ НЕСЕТЕ ПОЛНУЮ ОТВЕТСТВЕННОСТЬ ЗА:**
- Все финансовые потери
- Технические сбои
- Ошибки в работе бота
- Проблемы с биржей

**АВТОР НЕ НЕСЕТ ОТВЕТСТВЕННОСТИ ЗА ВАШИ ПОТЕРИ!**

**ИСПОЛЬЗУЙТЕ ТОЛЬКО ТЕ СРЕДСТВА, КОТОРЫЕ МОЖЕТЕ ПОЗВОЛИТЬ СЕБЕ ПОТЕРЯТЬ!**

---

Дата создания: 2026-02-11
© TRADERAGENT - Production Setup Guide
