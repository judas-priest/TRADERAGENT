// This source code is subject to the terms of the Mozilla Public License 2.0 at https://mozilla.org/MPL/2.0/
// © TRADERAGENT

//@version=6
indicator("ALMIR - Fibonacci Overlay", shorttitle="ALMIR Fib", overlay=true, max_lines_count=500, max_labels_count=500, max_boxes_count=500)

// ============================================================================
// ОПИСАНИЕ / DESCRIPTION
// ============================================================================
// Overlay компонент индикатора ALMIR для отображения уровней Фибоначчи на графике цены.
// Автоматически строит уровни от ключевых свечей, определенных основным индикатором.
//
// ALMIR overlay component for displaying Fibonacci levels on price chart.
// Automatically draws levels from key candles identified by main indicator.
// ============================================================================

// ============================================================================
// ВХОДНЫЕ ПАРАМЕТРЫ / INPUT PARAMETERS
// ============================================================================

// === RSI Settings (must match main indicator) / Настройки RSI (должны совпадать с основным индикатором) ===
rsiLength = input.int(10, "RSI Length / Период RSI", minval=1, group="📊 RSI Settings / Настройки RSI")
rsiSmoothLength = input.int(3, "RSI Smoothing / Сглаживание RSI", minval=1, group="📊 RSI Settings / Настройки RSI")
rsiOversold = input.int(30, "Oversold Level / Уровень перепроданности", minval=1, maxval=50, group="📊 RSI Settings / Настройки RSI")
rsiOverbought = input.int(70, "Overbought Level / Уровень перекупленности", minval=50, maxval=100, group="📊 RSI Settings / Настройки RSI")

// === Volume Settings / Настройки объема ===
volumeMaLength = input.int(20, "Volume MA Length / Период MA объема", minval=1, group="📊 Volume Settings / Настройки объема")
volumeMultiplier = input.float(1.5, "Volume Multiplier / Множитель объема", minval=1.0, step=0.1, group="📊 Volume Settings / Настройки объема")

// === Confluence Settings / Настройки Confluence ===
minConfluenceScore = input.int(5, "Min Confluence Score / Мин. балл confluence", minval=2, maxval=15, group="🎯 Confluence Settings / Настройки Confluence")
minBarsBetweenSignals = input.int(10, "Min Bars Between Signals / Мин. баров между сигналами", minval=1, group="🎯 Confluence Settings / Настройки Confluence")

// Indicator Weights / Веса индикаторов
rsiWeight = input.int(2, "RSI Weight / Вес RSI", minval=0, maxval=5, group="⚖️ Indicator Weights / Веса индикаторов")
macdWeight = input.int(2, "MACD Weight / Вес MACD", minval=0, maxval=5, group="⚖️ Indicator Weights / Веса индикаторов")
stochWeight = input.int(1, "Stochastic Weight / Вес Stochastic", minval=0, maxval=5, group="⚖️ Indicator Weights / Веса индикаторов")
volumeWeight = input.int(2, "Volume Weight / Вес объёма", minval=0, maxval=5, group="⚖️ Indicator Weights / Веса индикаторов")
priceActionWeight = input.int(1, "Price Action Weight / Вес Price Action", minval=0, maxval=5, group="⚖️ Indicator Weights / Веса индикаторов")

// === Fibonacci Settings / Настройки Фибоначчи ===
showFibLevels = input.bool(true, "Show Fibonacci Levels / Показать уровни Фибоначчи", group="📐 Fibonacci Settings / Настройки Фибоначчи")
fibExtension = input.int(50, "Fib Extension Bars / Продление баров", minval=10, maxval=200, group="📐 Fibonacci Settings / Настройки Фибоначчи")
showFibLabels = input.bool(true, "Show Fib Labels / Показать метки", group="📐 Fibonacci Settings / Настройки Фибоначчи")
fibLineWidth = input.int(1, "Fib Line Width / Толщина линий", minval=1, maxval=3, group="📐 Fibonacci Settings / Настройки Фибоначчи")

// Fibonacci Levels / Уровни Фибоначчи
showFib0 = input.bool(true, "Show 0.0 Level", group="📐 Fibonacci Levels / Уровни", inline="fib0")
showFib236 = input.bool(true, "Show 0.236 Level", group="📐 Fibonacci Levels / Уровни", inline="fib236")
showFib382 = input.bool(true, "Show 0.382 Level", group="📐 Fibonacci Levels / Уровни", inline="fib382")
showFib5 = input.bool(true, "Show 0.5 Level", group="📐 Fibonacci Levels / Уровни", inline="fib5")
showFib618 = input.bool(true, "Show 0.618 Level", group="📐 Fibonacci Levels / Уровни", inline="fib618")
showFib786 = input.bool(true, "Show 0.786 Level", group="📐 Fibonacci Levels / Уровни", inline="fib786")
showFib1 = input.bool(true, "Show 1.0 Level", group="📐 Fibonacci Levels / Уровни", inline="fib1")

// === Display Settings / Настройки отображения ===
showBullishSignals = input.bool(true, "Show Bullish Signals / Показать бычьи сигналы", group="🎨 Display Settings / Настройки отображения")
showBearishSignals = input.bool(true, "Show Bearish Signals / Показать медвежьи сигналы", group="🎨 Display Settings / Настройки отображения")
showSignalLabels = input.bool(true, "Show Signal Labels / Показать метки сигналов", group="🎨 Display Settings / Настройки отображения")

// ============================================================================
// SIGNAL DETECTION (simplified version from main indicator)
// ОПРЕДЕЛЕНИЕ СИГНАЛОВ (упрощенная версия из основного индикатора)
// ============================================================================

// RSI
rsi = ta.rsi(close, rsiLength)
rsiSmooth = ta.sma(rsi, rsiSmoothLength)

// Volume
volumeMa = ta.sma(volume, volumeMaLength)
volumeRatio = volume / volumeMa
strongVolume = volumeRatio > volumeMultiplier

// MACD
[macdLine, signalLine, _] = ta.macd(close, 10, 30, 20)
macdBullish = ta.crossover(macdLine, signalLine) and macdLine < 0
macdBearish = ta.crossunder(macdLine, signalLine) and macdLine > 0

// Stochastic
stochK = ta.stoch(close, high, low, 14)
stochD = ta.sma(stochK, 3)
stochBullish = ta.crossover(stochK, stochD) and stochK < 20
stochBearish = ta.crossunder(stochK, stochD) and stochK > 80

// Price Action
candleRange = high - low
candleBody = math.abs(close - open)
isBullishCandle = (close > open) and (candleBody > candleRange * 0.6) and candleRange > 0
isBearishCandle = (open > close) and (candleBody > candleRange * 0.6) and candleRange > 0

// Confluence Score
var int bullishScore = 0
var int bearishScore = 0

bullishScore := 0
bearishScore := 0

// RSI Signal
rsiBullishSignal = ta.crossover(rsi, rsiOversold) and rsiSmooth > rsiSmooth[1]
rsiBearishSignal = ta.crossunder(rsi, rsiOverbought) and rsiSmooth < rsiSmooth[1]

if rsiBullishSignal
    bullishScore += rsiWeight
if rsiBearishSignal
    bearishScore += rsiWeight

// MACD Signal
if macdBullish
    bullishScore += macdWeight
if macdBearish
    bearishScore += macdWeight

// Stochastic Signal
if stochBullish
    bullishScore += stochWeight
if stochBearish
    bearishScore += stochWeight

// Volume Signal
isBuyingPressure = close > open and strongVolume
isSellingPressure = close < open and strongVolume

if strongVolume and isBuyingPressure
    bullishScore += volumeWeight
if strongVolume and isSellingPressure
    bearishScore += volumeWeight

// Price Action Signal
if isBullishCandle
    bullishScore += priceActionWeight
if isBearishCandle
    bearishScore += priceActionWeight

// Track last signal bar
var int lastBullishBar = 0
var int lastBearishBar = 0

barsSinceLastBullish = bar_index - lastBullishBar
barsSinceLastBearish = bar_index - lastBearishBar

// Generate signals
bullishSignal = bullishScore >= minConfluenceScore and barsSinceLastBullish >= minBarsBetweenSignals and showBullishSignals
bearishSignal = bearishScore >= minConfluenceScore and barsSinceLastBearish >= minBarsBetweenSignals and showBearishSignals

// ============================================================================
// FIBONACCI LEVEL CALCULATION / РАСЧЁТ УРОВНЕЙ ФИБОНАЧЧИ
// ============================================================================

// Store signal candle data / Хранение данных свечи сигнала
var float bullishStartPrice = na
var float bullishEndPrice = na
var int bullishStartBar = na

var float bearishStartPrice = na
var float bearishEndPrice = na
var int bearishStartBar = na

// Update on new bullish signal / Обновление при новом бычьем сигнале
if bullishSignal
    lastBullishBar := bar_index
    bullishStartBar := bar_index
    bullishStartPrice := low  // Start from low of signal candle
    // Find recent high for Fibonacci range
    bullishEndPrice := ta.highest(high, 20)  // Look back 20 bars for high

// Update on new bearish signal / Обновление при новом медвежьем сигнале
if bearishSignal
    lastBearishBar := bar_index
    bearishStartBar := bar_index
    bearishStartPrice := high  // Start from high of signal candle
    // Find recent low for Fibonacci range
    bearishEndPrice := ta.lowest(low, 20)  // Look back 20 bars for low

// ============================================================================
// DRAW FIBONACCI LEVELS / ОТРИСОВКА УРОВНЕЙ ФИБОНАЧЧИ
// ============================================================================

// Function to draw Fibonacci level / Функция отрисовки уровня Фибоначчи
drawFibLevel(startPrice, endPrice, level, levelName, levelColor, startBar) =>
    if showFibLevels and not na(startPrice) and not na(endPrice)
        price = startPrice + (endPrice - startPrice) * level
        currentBar = bar_index
        barsFromSignal = currentBar - startBar

        // Only draw if within extension range
        if barsFromSignal <= fibExtension
            line fibLine = line.new(startBar, price, currentBar, price,
                 color=levelColor, width=fibLineWidth, style=line.style_solid)

            if showFibLabels and barsFromSignal == 0
                label.new(startBar, price, levelName,
                     color=color.new(levelColor, 80), textcolor=levelColor,
                     style=label.style_label_left, size=size.small)

// Draw Bullish Fibonacci Levels / Отрисовка бычьих уровней Фибоначчи
if not na(bullishStartPrice) and not na(bullishEndPrice) and not na(bullishStartBar)
    barsFromSignal = bar_index - bullishStartBar
    if barsFromSignal <= fibExtension
        if showFib0
            drawFibLevel(bullishStartPrice, bullishEndPrice, 0.0, "0.0", color.new(color.red, 0), bullishStartBar)
        if showFib236
            drawFibLevel(bullishStartPrice, bullishEndPrice, 0.236, "0.236", color.new(color.orange, 0), bullishStartBar)
        if showFib382
            drawFibLevel(bullishStartPrice, bullishEndPrice, 0.382, color.new(color.yellow, 0), "0.382", bullishStartBar)
        if showFib5
            drawFibLevel(bullishStartPrice, bullishEndPrice, 0.5, "0.5", color.new(color.blue, 0), bullishStartBar)
        if showFib618
            drawFibLevel(bullishStartPrice, bullishEndPrice, 0.618, "0.618", color.new(color.purple, 0), bullishStartBar)
        if showFib786
            drawFibLevel(bullishStartPrice, bullishEndPrice, 0.786, "0.786", color.new(color.teal, 0), bullishStartBar)
        if showFib1
            drawFibLevel(bullishStartPrice, bullishEndPrice, 1.0, "1.0", color.new(color.green, 0), bullishStartBar)

// Draw Bearish Fibonacci Levels / Отрисовка медвежьих уровней Фибоначчи
if not na(bearishStartPrice) and not na(bearishEndPrice) and not na(bearishStartBar)
    barsFromSignal = bar_index - bearishStartBar
    if barsFromSignal <= fibExtension
        if showFib0
            drawFibLevel(bearishStartPrice, bearishEndPrice, 0.0, "0.0", color.new(color.green, 0), bearishStartBar)
        if showFib236
            drawFibLevel(bearishStartPrice, bearishEndPrice, 0.236, "0.236", color.new(color.teal, 0), bearishStartBar)
        if showFib382
            drawFibLevel(bearishStartPrice, bearishEndPrice, 0.382, "0.382", color.new(color.purple, 0), bearishStartBar)
        if showFib5
            drawFibLevel(bearishStartPrice, bearishEndPrice, 0.5, "0.5", color.new(color.blue, 0), bearishStartBar)
        if showFib618
            drawFibLevel(bearishStartPrice, bearishEndPrice, 0.618, "0.618", color.new(color.yellow, 0), bearishStartBar)
        if showFib786
            drawFibLevel(bearishStartPrice, bearishEndPrice, 0.786, "0.786", color.new(color.orange, 0), bearishStartBar)
        if showFib1
            drawFibLevel(bearishStartPrice, bearishEndPrice, 1.0, "1.0", color.new(color.red, 0), bearishStartBar)

// ============================================================================
// MARK KEY CANDLES ON CHART / ОТМЕТКИ КЛЮЧЕВЫХ СВЕЧЕЙ НА ГРАФИКЕ
// ============================================================================

// Mark bullish signal candles / Отметка бычьих сигнальных свечей
if showSignalLabels and bullishSignal
    label.new(bar_index, low, "🟢\nFIB",
         color=color.new(color.green, 0), textcolor=color.white,
         style=label.style_label_up, size=size.normal,
         tooltip="Bullish Signal\nScore: " + str.tostring(bullishScore))

// Mark bearish signal candles / Отметка медвежьих сигнальных свечей
if showSignalLabels and bearishSignal
    label.new(bar_index, high, "🔴\nFIB",
         color=color.new(color.red, 0), textcolor=color.white,
         style=label.style_label_down, size=size.normal,
         tooltip="Bearish Signal\nScore: " + str.tostring(bearishScore))

// ============================================================================
// BACKGROUND HIGHLIGHTS / ПОДСВЕТКА ФОНА
// ============================================================================

// Highlight signal candles / Подсветка сигнальных свечей
bgcolor(bullishSignal ? color.new(color.green, 90) : na, title="Bullish Signal BG")
bgcolor(bearishSignal ? color.new(color.red, 90) : na, title="Bearish Signal BG")
