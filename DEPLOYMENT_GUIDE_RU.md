# Руководство по развертыванию TRADERAGENT DCA-Grid Bot

**Версия:** 1.0
**Дата:** 2026-02-09
**Статус:** Готов к развертыванию

---

## 📋 Содержание

1. [Введение](#введение)
2. [План развертывания](#план-развертывания)
3. [Требования со стороны пользователя](#требования-со-стороны-пользователя)
4. [Пошаговая инструкция по развертыванию](#пошаговая-инструкция-по-развертыванию)
5. [Проверка работоспособности](#проверка-работоспособности)
6. [Управление ботом](#управление-ботом)
7. [Мониторинг и обслуживание](#мониторинг-и-обслуживание)
8. [Решение проблем](#решение-проблем)
9. [Рекомендации по безопасности](#рекомендации-по-безопасности)

---

## Введение

Данное руководство предназначено для развертывания автономного DCA-сеточного торгового бота на удаленном сервере (VPS/VDS). Бот был разработан в течение 5 этапов и готов к тестированию в production-подобной среде.

### Что было сделано

✅ **Этап 1**: Базовая инфраструктура (Database, Config, Logger)
✅ **Этап 2**: Торговые модули (Grid Engine, DCA Engine, Risk Manager)
✅ **Этап 3**: Интеграция и оркестрация (Bot Orchestrator, Exchange Client, Telegram Bot)
✅ **Этап 4**: Тестирование (Unit tests, Integration tests, Backtesting, Testnet)
✅ **Этап 5**: Документация (README, Configuration, Deployment guides)

### Текущий этап

🚀 **Этап 6**: Развертывание на удаленном сервере и тестирование в реальных условиях

---

## План развертывания

### Этап 6.1: Подготовка к развертыванию (1-2 дня)

**Цель**: Подготовить все необходимое для развертывания

**Задачи для пользователя**:
1. Арендовать VPS/VDS сервер (минимум 2GB RAM, 2 CPU cores, 20GB SSD)
2. Получить доступ к серверу по SSH
3. Зарегистрировать аккаунт на криптобирже (Binance/Bybit/OKX)
4. Получить API ключи от биржи (с ограничениями на withdrawal)
5. Создать Telegram бота через @BotFather
6. Подготовить начальный капитал для тестирования

**Задачи для разработчика**:
1. Проверить актуальность всей документации
2. Подготовить скрипты для автоматического развертывания
3. Создать проверочные скрипты для валидации установки
4. Подготовить примеры конфигураций для разных сценариев

### Этап 6.2: Установка на сервер (2-4 часа)

**Цель**: Развернуть бота на удаленном сервере

**Задачи**:
1. Подключение к серверу
2. Установка необходимого ПО (Docker, Docker Compose)
3. Клонирование репозитория
4. Настройка переменных окружения
5. Настройка конфигурации бота
6. Запуск инфраструктуры (PostgreSQL, Redis)
7. Запуск бота

### Этап 6.3: Тестирование на Testnet (3-7 дней)

**Цель**: Проверить работу бота в безопасной testnet среде

**Задачи**:
1. Настройка подключения к testnet биржи
2. Пополнение testnet аккаунта (faucet)
3. Запуск бота в testnet режиме
4. Мониторинг работы бота
5. Проверка выполнения ордеров
6. Анализ логов и метрик
7. Тестирование различных сценариев (Grid, DCA, Hybrid)
8. Проверка уведомлений Telegram
9. Тестирование аварийных ситуаций
10. Документирование результатов

### Этап 6.4: Подготовка к production (1-2 дня)

**Цель**: Подготовить бота к работе с реальными средствами

**Задачи**:
1. Анализ результатов testnet тестирования
2. Корректировка параметров на основе результатов
3. Настройка production конфигурации
4. Настройка системы мониторинга (Prometheus + Grafana)
5. Настройка автоматических бэкапов базы данных
6. Настройка алертов в Telegram
7. Подготовка плана действий в чрезвычайных ситуациях

### Этап 6.5: Production запуск с минимальным капиталом (7-14 дней)

**Цель**: Запустить бота с минимальными средствами для проверки

**Задачи**:
1. Перевод API ключей на production
2. Настройка sandbox: false
3. Первоначальный запуск с dry_run: true
4. Проверка расчетов в dry_run режиме
5. Запуск с минимальным капиталом (рекомендуется $50-100)
6. Ежедневный мониторинг работы
7. Анализ производительности
8. Постепенное увеличение капитала при успешной работе

### Этап 6.6: Масштабирование и оптимизация (ongoing)

**Цель**: Оптимизация параметров и масштабирование

**Задачи**:
1. Анализ статистики торговли
2. Оптимизация параметров стратегий
3. Увеличение торгового капитала
4. Добавление новых торговых пар
5. Тестирование дополнительных стратегий
6. Регулярные обновления и улучшения

---

## Требования со стороны пользователя

### Технические требования

#### 1. VPS/VDS Сервер

**Минимальные требования**:
- **CPU**: 2 ядра
- **RAM**: 2 GB
- **Диск**: 20 GB SSD
- **ОС**: Ubuntu 20.04/22.04 или Debian 11+
- **Сеть**: 100 Mbps, стабильное соединение
- **Доступ**: SSH root или sudo доступ

**Рекомендуемые требования**:
- **CPU**: 4 ядра
- **RAM**: 4 GB
- **Диск**: 40 GB SSD
- **Сеть**: 1 Gbps
- **Backup**: Автоматические бэкапы

**Рекомендуемые провайдеры**:
- DigitalOcean (от $12/месяц)
- Linode (от $12/месяц)
- Vultr (от $10/месяц)
- Hetzner (от €5/месяц)
- Contabo (от €5/месяц)

#### 2. Биржевой аккаунт

**Поддерживаемые биржи**:
- Binance (рекомендуется)
- Bybit
- OKX
- И другие через CCXT (150+ бирж)

**Требования к API ключам**:
- ✅ Spot Trading (торговля на споте)
- ✅ Read Permissions (чтение данных)
- ❌ Withdrawal (ОТКЛЮЧИТЬ вывод средств)
- ✅ IP Whitelist (рекомендуется привязать к IP сервера)

**Ограничения**:
- Установить максимальные лимиты на сделки
- Ограничить торговлю только спотом
- Запретить маржинальную торговлю
- Запретить фьючерсы

#### 3. Telegram

**Требования**:
- Аккаунт Telegram
- Создать бота через [@BotFather](https://t.me/botfather)
- Получить Chat ID через [@userinfobot](https://t.me/userinfobot)

#### 4. Начальный капитал

**Для testnet**:
- Используйте testnet faucet для получения тестовых монет
- Не требуется реальных денег

**Для production (минимальный старт)**:
- **Рекомендуется**: $100-500 для первого теста
- **Минимум**: $50 (но комиссии могут быть непропорционально высокими)
- **Оптимально**: $1000+ для эффективной работы стратегий

**Распределение капитала**:
- Grid Trading: 60-70% капитала
- DCA Reserve: 30-40% капитала
- Emergency Reserve: держите дополнительно 20% вне бота

### Знания и навыки

**Необходимый минимум**:
- Базовые навыки работы с командной строкой Linux
- Понимание SSH подключения
- Умение копировать и редактировать текстовые файлы
- Базовое понимание Docker (желательно)

**Не обязательно, но полезно**:
- Опыт работы с криптовалютами
- Понимание торговых стратегий Grid и DCA
- Навыки работы с базами данных
- Опыт мониторинга приложений

### Временные затраты

**Первоначальная настройка**: 3-5 часов
- Аренда сервера: 30 минут
- Установка ПО: 1 час
- Настройка конфигурации: 1-2 часа
- Первый запуск и проверка: 1 час

**Testnet тестирование**: 3-7 дней
- Минимальный активный мониторинг: 30 минут/день
- Проверка логов и метрик

**Production запуск**: 1-2 часа
- Перенастройка на production
- Запуск с минимальным капиталом

**Регулярное обслуживание**: 15-30 минут/день
- Проверка статуса бота
- Просмотр уведомлений Telegram
- Анализ производительности

---

## Пошаговая инструкция по развертыванию

### Шаг 1: Подготовка сервера

#### 1.1. Подключение к серверу

```bash
# Подключитесь к вашему серверу по SSH
ssh root@ваш_сервер_ip

# Или с использованием пользователя с sudo
ssh username@ваш_сервер_ip
```

#### 1.2. Обновление системы

```bash
# Обновите список пакетов
sudo apt update

# Обновите установленные пакеты
sudo apt upgrade -y

# Установите базовые утилиты
sudo apt install -y curl wget git nano htop net-tools
```

#### 1.3. Установка Docker

```bash
# Установите Docker
curl -fsSL https://get.docker.com -o get-docker.sh
sudo sh get-docker.sh

# Добавьте вашего пользователя в группу docker
sudo usermod -aG docker $USER

# Примените изменения группы
newgrp docker

# Проверьте установку Docker
docker --version
# Ожидаемый вывод: Docker version 24.x.x, build xxxxxxx
```

#### 1.4. Установка Docker Compose

```bash
# Установите Docker Compose плагин
sudo apt-get install -y docker-compose-plugin

# Проверьте установку
docker compose version
# Ожидаемый вывод: Docker Compose version v2.x.x
```

#### 1.5. Настройка файрвола (опционально, но рекомендуется)

```bash
# Установите UFW (Uncomplicated Firewall)
sudo apt install -y ufw

# Разрешите SSH (ВАЖНО: сделайте это до включения файрвола!)
sudo ufw allow 22/tcp

# Включите файрвол
sudo ufw enable

# Проверьте статус
sudo ufw status
```

### Шаг 2: Клонирование репозитория

```bash
# Создайте директорию для проектов
mkdir -p ~/projects
cd ~/projects

# Клонируйте репозиторий
git clone https://github.com/alekseymavai/TRADERAGENT.git

# Перейдите в директорию проекта
cd TRADERAGENT

# Проверьте структуру проекта
ls -la
```

### Шаг 3: Настройка переменных окружения

#### 3.1. Создание .env файла

```bash
# Скопируйте пример конфигурации
cp .env.example .env

# Откройте файл для редактирования
nano .env
```

#### 3.2. Заполнение переменных окружения

Отредактируйте `.env` файл следующим образом:

```env
# ============================================
# Database Configuration
# ============================================
DB_USER=traderagent
DB_PASSWORD=ВАШ_НАДЁЖНЫЙ_ПАРОЛЬ_ДЛЯ_БД
DB_NAME=traderagent
DB_PORT=5432

# ============================================
# Redis Configuration
# ============================================
REDIS_PORT=6379

# ============================================
# Bot Configuration
# ============================================
CONFIG_FILE=production.yaml
LOG_LEVEL=INFO

# ============================================
# Telegram Bot Configuration
# ============================================
# Получите токен от @BotFather
TELEGRAM_BOT_TOKEN=123456789:ABCdefGHIjklMNOpqrsTUVwxyz

# Получите ваш Chat ID от @userinfobot
TELEGRAM_ALLOWED_CHAT_IDS=123456789

# ============================================
# Security
# ============================================
# Сгенерируйте ключ шифрования (см. ниже)
ENCRYPTION_KEY=ВАШ_СГЕНЕРИРОВАННЫЙ_КЛЮЧ
```

**Сохраните файл**: `Ctrl+O`, затем `Enter`, затем `Ctrl+X`

#### 3.3. Генерация ключа шифрования

```bash
# Сгенерируйте безопасный ключ шифрования
python3 -c "import os, base64; print(base64.b64encode(os.urandom(32)).decode())"

# Скопируйте полученный ключ и вставьте его в .env как ENCRYPTION_KEY
```

### Шаг 4: Настройка конфигурации бота

#### 4.1. Создание конфигурационного файла

```bash
# Скопируйте пример конфигурации
cp configs/example.yaml configs/production.yaml

# Откройте файл для редактирования
nano configs/production.yaml
```

#### 4.2. Базовая конфигурация для testnet

Вот пример минимальной рабочей конфигурации для **testnet тестирования**:

```yaml
# Database URL (автоматически устанавливается в Docker)
database_url: postgresql+asyncpg://traderagent:password@postgres:5432/traderagent
database_pool_size: 5

# Logging
log_level: INFO
log_to_file: true
log_to_console: true
json_logs: false

# Encryption key (из .env)
encryption_key: ${ENCRYPTION_KEY}

# Bot Configurations
bots:
  # Тестовый Grid бот на BTC/USDT
  - version: 1
    name: btc_testnet_grid
    symbol: BTC/USDT
    strategy: grid

    exchange:
      exchange_id: binance
      credentials_name: binance_testnet
      sandbox: true  # ВАЖНО: используем testnet!
      rate_limit: true

    grid:
      enabled: true
      upper_price: "50000"    # Верхняя граница сетки
      lower_price: "40000"    # Нижняя граница сетки
      grid_levels: 10         # Количество уровней
      amount_per_grid: "10"   # Объем на уровень (в USDT)
      profit_per_grid: "0.01" # 1% прибыли на уровень

    risk_management:
      max_position_size: "500"      # Максимальная позиция в USDT
      stop_loss_percentage: "0.15"  # 15% stop loss
      min_order_size: "10"          # Минимальный размер ордера

    notifications:
      enabled: true
      notify_on_trade: true
      notify_on_error: true

    dry_run: true      # Начинаем с симуляции!
    auto_start: false  # Ручной запуск через Telegram
```

**Сохраните файл**: `Ctrl+O`, `Enter`, `Ctrl+X`

#### 4.3. Добавление API ключей биржи

API ключи НЕ хранятся в конфигурационном файле. Они добавляются через Telegram бота после запуска или через базу данных с шифрованием.

**Подготовьте**:
- API Key от вашей биржи
- API Secret от вашей биржи
- Убедитесь, что ключи настроены только для testnet (sandbox)

### Шаг 5: Развертывание бота

#### 5.1. Проверка конфигурации

```bash
# Проверьте, что .env файл настроен
cat .env | grep -v "^#" | grep -v "^$"

# Проверьте, что production.yaml существует
ls -la configs/production.yaml
```

#### 5.2. Запуск развертывания

```bash
# Сделайте скрипт развертывания исполняемым
chmod +x deploy.sh

# Запустите автоматическое развертывание
./deploy.sh
```

**Скрипт deploy.sh выполнит**:
1. ✅ Проверку prerequisites (Docker, Docker Compose)
2. ✅ Проверку .env конфигурации
3. ✅ Проверку production.yaml
4. ✅ Сборку Docker образов
5. ✅ Запуск PostgreSQL и Redis
6. ✅ Выполнение миграций базы данных
7. ✅ Запуск бота
8. ✅ Показ статуса и логов

#### 5.3. Ожидаемый вывод

Успешное развертывание должно показать:

```
🚀 TRADERAGENT Bot Deployment
==============================
[INFO] Checking prerequisites...
[INFO] Prerequisites check passed ✓
[INFO] Checking environment configuration...
[INFO] Environment configuration check passed ✓
[INFO] Checking bot configuration...
[INFO] Bot configuration check passed ✓
[INFO] Building Docker images...
[INFO] Docker images built successfully ✓
[INFO] Starting services...
[INFO] Services started successfully ✓
[INFO] Running database migrations...
[INFO] Database migrations completed ✓
[INFO] Starting trading bot...
[INFO] Trading bot started successfully ✓

[INFO] Deployment Status:
NAME                    STATUS
traderagent-bot         Up (healthy)
traderagent-postgres    Up (healthy)
traderagent-redis       Up (healthy)

[INFO] Recent bot logs:
...
[INFO] Deployment completed successfully! 🎉
```

### Шаг 6: Добавление API ключей через Telegram

#### 6.1. Найдите вашего бота в Telegram

Откройте Telegram и найдите вашего бота по username, который вы создали через @BotFather.

#### 6.2. Начните общение с ботом

```
Отправьте: /start
```

Бот ответит списком доступных команд.

#### 6.3. Добавьте учетные данные биржи

```
Отправьте: /add_credentials
```

Следуйте инструкциям бота:
1. Введите название (например: `binance_testnet`)
2. Введите API Key
3. Введите API Secret
4. Выберите биржу (Binance)
5. Подтвердите, что это testnet

**Важно**: API ключи будут зашифрованы и сохранены в базе данных. Они НЕ сохраняются в открытом виде.

### Шаг 7: Запуск бота

#### 7.1. Проверка списка ботов

```
Отправьте в Telegram: /list
```

Вы должны увидеть ваш бот `btc_testnet_grid` со статусом `Stopped`.

#### 7.2. Запуск бота

```
Отправьте в Telegram: /start_bot btc_testnet_grid
```

Бот начнет работу и отправит уведомление о запуске.

#### 7.3. Проверка статуса

```
Отправьте в Telegram: /status btc_testnet_grid
```

Вы увидите текущий статус бота, количество открытых ордеров, баланс и другую информацию.

---

## Проверка работоспособности

### Проверка 1: Контейнеры запущены

```bash
# Проверьте статус всех контейнеров
docker-compose ps

# Все контейнеры должны быть в статусе "Up" или "Up (healthy)"
```

### Проверка 2: Логи бота

```bash
# Просмотр логов в реальном времени
docker-compose logs -f bot

# Просмотр последних 100 строк логов
docker-compose logs --tail=100 bot
```

**Что искать в логах**:
- ✅ `Bot Orchestrator initialized successfully`
- ✅ `Connected to exchange: binance (sandbox: True)`
- ✅ `Database connection established`
- ✅ `Telegram bot started`
- ❌ Отсутствие ERROR сообщений

### Проверка 3: База данных

```bash
# Подключитесь к PostgreSQL
docker-compose exec postgres psql -U traderagent -d traderagent

# Проверьте таблицы
\dt

# Проверьте конфигурации ботов
SELECT id, name, symbol, strategy, status FROM bot_configs;

# Выйдите из psql
\q
```

### Проверка 4: Redis

```bash
# Проверьте Redis
docker-compose exec redis redis-cli ping

# Ожидаемый ответ: PONG
```

### Проверка 5: Telegram бот

```bash
# Проверьте логи Telegram бота
docker-compose logs bot | grep -i telegram

# Должно быть: "Telegram bot started successfully"
```

### Проверка 6: Exchange подключение

Отправьте в Telegram:
```
/balance btc_testnet_grid
```

Если бот отвечает с информацией о балансе (даже если он 0), подключение к бирже работает.

### Проверка 7: Мониторинг ресурсов

```bash
# Просмотр использования ресурсов контейнерами
docker stats

# Проверьте использование диска
df -h

# Проверьте использование памяти
free -h
```

---

## Управление ботом

### Команды Telegram бота

#### Информационные команды

```
/start          - Показать справку
/list           - Список всех ботов
/status <name>  - Статус конкретного бота
/balance <name> - Баланс и позиции
/orders <name>  - Открытые ордера
/history <name> - История сделок (последние 10)
/pnl <name>     - Profit & Loss статистика
```

#### Команды управления

```
/start_bot <name>  - Запустить бота
/stop_bot <name>   - Остановить бота
/pause <name>      - Приостановить торговлю
/resume <name>     - Возобновить торговлю
/emergency_stop    - Экстренная остановка ВСЕХ ботов
```

#### Команды конфигурации

```
/add_credentials          - Добавить API ключи биржи
/list_credentials         - Список сохраненных ключей
/reload_config           - Перезагрузить конфигурацию
```

### Команды Docker

#### Просмотр логов

```bash
# Логи бота в реальном времени
docker-compose logs -f bot

# Логи PostgreSQL
docker-compose logs -f postgres

# Логи Redis
docker-compose logs -f redis

# Все логи
docker-compose logs -f
```

#### Перезапуск сервисов

```bash
# Перезапуск бота
docker-compose restart bot

# Перезапуск всех сервисов
docker-compose restart

# Остановка всех сервисов
docker-compose down

# Запуск всех сервисов
docker-compose up -d
```

#### Обновление бота

```bash
# Остановите бота
docker-compose down

# Получите последние изменения
git pull

# Пересоберите образы
docker-compose build

# Запустите снова
docker-compose up -d

# Примените миграции (если есть)
docker-compose run --rm migrations
```

### Резервное копирование

#### Backup базы данных

```bash
# Создайте backup
docker-compose exec postgres pg_dump -U traderagent traderagent > backup_$(date +%Y%m%d_%H%M%S).sql

# Или используйте скрипт
mkdir -p ~/backups
docker-compose exec postgres pg_dump -U traderagent traderagent > ~/backups/traderagent_$(date +%Y%m%d_%H%M%S).sql
```

#### Восстановление из backup

```bash
# Восстановите из backup файла
docker-compose exec -T postgres psql -U traderagent traderagent < ~/backups/traderagent_20260209_120000.sql
```

#### Автоматический backup (cron)

```bash
# Откройте crontab
crontab -e

# Добавьте задачу для ежедневного backup в 3:00 AM
0 3 * * * cd ~/projects/TRADERAGENT && docker-compose exec postgres pg_dump -U traderagent traderagent > ~/backups/traderagent_$(date +\%Y\%m\%d).sql

# Добавьте задачу для очистки старых backups (старше 30 дней)
0 4 * * * find ~/backups -name "traderagent_*.sql" -mtime +30 -delete
```

---

## Мониторинг и обслуживание

### Ежедневный мониторинг

**Рекомендуемая рутина (5-10 минут/день)**:

1. **Проверка статуса через Telegram**
   ```
   /list
   /status <bot_name>
   /pnl <bot_name>
   ```

2. **Проверка уведомлений**
   - Просмотрите уведомления от бота в Telegram
   - Убедитесь, что нет ERROR сообщений

3. **Проверка логов** (если есть подозрения на проблемы)
   ```bash
   docker-compose logs --tail=50 bot
   ```

4. **Проверка системных ресурсов**
   ```bash
   docker stats --no-stream
   df -h
   ```

### Еженедельное обслуживание

1. **Анализ производительности**
   ```
   /pnl <bot_name>
   /history <bot_name>
   ```

2. **Проверка логов на ошибки**
   ```bash
   docker-compose logs bot | grep -i error | tail -50
   docker-compose logs bot | grep -i warning | tail -50
   ```

3. **Backup базы данных**
   ```bash
   cd ~/projects/TRADERAGENT
   mkdir -p ~/backups
   docker-compose exec postgres pg_dump -U traderagent traderagent > ~/backups/weekly_$(date +%Y%m%d).sql
   ```

4. **Проверка дискового пространства**
   ```bash
   df -h
   docker system df
   ```

### Ежемесячное обслуживание

1. **Обновление системы**
   ```bash
   sudo apt update && sudo apt upgrade -y
   ```

2. **Обновление Docker образов**
   ```bash
   cd ~/projects/TRADERAGENT
   docker-compose down
   git pull
   docker-compose build --no-cache
   docker-compose up -d
   ```

3. **Очистка неиспользуемых Docker ресурсов**
   ```bash
   docker system prune -a --volumes
   # ВНИМАНИЕ: Это удалит неиспользуемые контейнеры, образы и volumes
   # Убедитесь, что у вас есть backup!
   ```

4. **Проверка и ротация логов**
   ```bash
   # Проверьте размер логов
   du -sh /var/lib/docker/containers/*/

   # Docker автоматически ротирует логи, но можно настроить лимиты
   # в docker-compose.yml (уже настроено)
   ```

### Мониторинг метрик (опционально)

Если вы хотите более продвинутый мониторинг:

```bash
# Запустите Prometheus + Grafana
docker-compose -f docker-compose.monitoring.yml up -d

# Откройте в браузере:
# Grafana: http://ваш_сервер_ip:3000 (admin/admin)
# Prometheus: http://ваш_сервер_ip:9090
```

**Настройка Grafana**:
1. Первый вход: admin/admin
2. Смените пароль
3. Добавьте Prometheus data source: http://prometheus:9090
4. Импортируйте дашборды из monitoring/grafana/dashboards/

---

## Решение проблем

### Проблема 1: Бот не запускается

**Симптомы**: `docker-compose ps` показывает, что контейнер bot постоянно перезапускается.

**Диагностика**:
```bash
# Проверьте логи
docker-compose logs bot

# Проверьте последние 50 строк
docker-compose logs --tail=50 bot
```

**Возможные причины и решения**:

1. **Ошибка в конфигурации**
   ```
   Лог: "ConfigurationError: ..."
   Решение: Проверьте configs/production.yaml на ошибки
   ```

2. **Не может подключиться к базе данных**
   ```
   Лог: "could not connect to server: Connection refused"
   Решение:
   - Проверьте, что postgres контейнер запущен: docker-compose ps postgres
   - Перезапустите postgres: docker-compose restart postgres
   - Подождите 10 секунд и перезапустите бота: docker-compose restart bot
   ```

3. **Неверный ENCRYPTION_KEY**
   ```
   Лог: "DecryptionError: ..."
   Решение: Сгенерируйте новый ключ и обновите .env
   ```

4. **Недостаточно памяти**
   ```
   Лог: "MemoryError" или контейнер killed
   Решение:
   - Проверьте: free -h
   - Увеличьте RAM на сервере
   - Уменьшите database_pool_size в конфиге
   ```

### Проблема 2: Telegram бот не отвечает

**Диагностика**:
```bash
# Проверьте логи Telegram
docker-compose logs bot | grep -i telegram

# Проверьте, что бот запущен
docker-compose ps bot
```

**Возможные причины**:

1. **Неверный TELEGRAM_BOT_TOKEN**
   ```
   Лог: "Unauthorized" или "Invalid token"
   Решение: Проверьте токен в .env, получите новый от @BotFather если нужно
   ```

2. **Неверный TELEGRAM_ALLOWED_CHAT_IDS**
   ```
   Лог: "Access denied for chat_id: ..."
   Решение:
   - Получите ваш chat_id от @userinfobot
   - Обновите TELEGRAM_ALLOWED_CHAT_IDS в .env
   - Перезапустите: docker-compose restart bot
   ```

3. **Telegram API недоступен**
   ```
   Лог: "Failed to connect to api.telegram.org"
   Решение: Проверьте интернет соединение сервера
   ```

### Проблема 3: Не удается подключиться к бирже

**Диагностика через Telegram**:
```
/status <bot_name>
```

**Ошибки**:

1. **Invalid API Key**
   ```
   Решение:
   - Проверьте, что API ключи правильно введены
   - Убедитесь, что ключи активированы на бирже
   - Проверьте IP whitelist (если настроен на бирже)
   - Пересоздайте API ключи на бирже
   - Удалите и заново добавьте через /add_credentials
   ```

2. **Exchange not available / Sandbox mode**
   ```
   Решение:
   - Убедитесь, что sandbox: true в конфиге для testnet
   - Убедитесь, что testnet биржи доступен
   - Binance testnet: https://testnet.binance.vision/
   - Проверьте статус: https://status.binance.com/
   ```

3. **Rate Limit Exceeded**
   ```
   Решение:
   - Уменьшите частоту запросов
   - Увеличьте интервал обновления цен в конфиге
   - Подождите несколько минут
   ```

### Проблема 4: База данных переполнена

**Симптомы**: Диск заполнен, база данных не принимает запросы.

**Диагностика**:
```bash
# Проверьте дисковое пространство
df -h

# Проверьте размер базы данных
docker-compose exec postgres psql -U traderagent -d traderagent -c "SELECT pg_size_pretty(pg_database_size('traderagent'));"
```

**Решение**:

1. **Очистка старых логов событий**
   ```bash
   docker-compose exec postgres psql -U traderagent -d traderagent -c "DELETE FROM event_logs WHERE created_at < NOW() - INTERVAL '30 days';"
   ```

2. **Очистка старых сделок**
   ```bash
   docker-compose exec postgres psql -U traderagent -d traderagent -c "DELETE FROM trades WHERE created_at < NOW() - INTERVAL '90 days';"
   ```

3. **Vacuum базы данных**
   ```bash
   docker-compose exec postgres psql -U traderagent -d traderagent -c "VACUUM FULL;"
   ```

### Проблема 5: Высокое использование CPU/RAM

**Диагностика**:
```bash
# Проверьте использование ресурсов
docker stats

# Проверьте системные ресурсы
htop
```

**Решение**:

1. **Слишком много ботов**
   - Остановите неиспользуемые боты
   - Уменьшите количество одновременно работающих ботов

2. **Слишком частое обновление**
   - Увеличьте интервалы обновления в конфиге
   - Уменьшите частоту проверки цен

3. **Недостаточно ресурсов сервера**
   - Обновите VPS план на более мощный
   - Оптимизируйте параметры database_pool_size

### Проблема 6: Ордера не выставляются

**Диагностика**:
```
/orders <bot_name>
```

Если ордеров нет, проверьте:

1. **Dry run режим включен**
   ```yaml
   # В configs/production.yaml
   dry_run: true  # Измените на false для реальной торговли
   ```

2. **Недостаточный баланс**
   ```
   /balance <bot_name>

   Решение: Пополните баланс на бирже
   ```

3. **Неверные параметры сетки**
   ```
   Лог: "Grid configuration invalid: upper_price must be > lower_price"
   Решение: Проверьте параметры grid в конфиге
   ```

4. **Минимальный размер ордера**
   ```
   Лог: "Order size below minimum: ..."
   Решение: Увеличьте amount_per_grid в конфиге
   ```

---

## Рекомендации по безопасности

### 1. Безопасность сервера

#### SSH безопасность

```bash
# Отключите вход по паролю, используйте только SSH ключи
sudo nano /etc/ssh/sshd_config

# Установите:
PasswordAuthentication no
PermitRootLogin no

# Перезапустите SSH
sudo systemctl restart sshd
```

#### Автоматические обновления безопасности

```bash
# Установите автоматические обновления
sudo apt install -y unattended-upgrades

# Включите автоматические обновления
sudo dpkg-reconfigure -plow unattended-upgrades
```

#### Fail2ban для защиты от brute-force

```bash
# Установите fail2ban
sudo apt install -y fail2ban

# Создайте локальную конфигурацию
sudo cp /etc/fail2ban/jail.conf /etc/fail2ban/jail.local

# Запустите fail2ban
sudo systemctl enable fail2ban
sudo systemctl start fail2ban
```

### 2. Безопасность API ключей

**КРИТИЧЕСКИ ВАЖНО**:

✅ **Делать**:
- Создавайте отдельные API ключи для бота
- Ограничивайте права ключей (только Spot Trading + Read)
- ЗАПРЕЩАЙТЕ Withdrawal (вывод средств)
- Используйте IP Whitelist (привязка к IP сервера)
- Устанавливайте лимиты на максимальный размер ордера
- Регулярно ротируйте ключи (каждые 3-6 месяцев)

❌ **НЕ делать**:
- НЕ используйте основной API ключ
- НЕ давайте права на withdrawal
- НЕ давайте права на маржинальную торговлю
- НЕ давайте права на фьючерсы
- НЕ храните ключи в открытом виде в файлах
- НЕ коммитьте ключи в git

### 3. Безопасность конфигурации

```bash
# Установите правильные права на файлы
chmod 600 .env
chmod 600 configs/production.yaml

# Убедитесь, что .env в .gitignore
echo ".env" >> .gitignore
echo "configs/production.yaml" >> .gitignore
```

### 4. Мониторинг безопасности

#### Логи входов в систему

```bash
# Проверяйте логи входов
sudo tail -50 /var/log/auth.log

# Проверяйте активные SSH сессии
who
```

#### Алерты на подозрительную активность

Настройте Telegram уведомления:
- Большие потери (drawdown)
- Частые ошибки API
- Неудачные попытки входа в систему
- Необычные объемы торговли

### 5. Backup стратегия

**3-2-1 правило**:
- 3 копии данных
- 2 разных типа медиа
- 1 копия off-site

```bash
# Локальный backup (на сервере)
~/backups/

# Удаленный backup (rsync на другой сервер или local машину)
rsync -avz ~/backups/ user@backup-server:~/traderagent-backups/

# Cloud backup (например, на S3)
# Установите AWS CLI и настройте автоматический upload
```

### 6. Управление рисками

**Финансовые риски**:

1. **Начинайте с малого**
   - Первый production запуск: $50-100
   - После недели успешной работы: до $500
   - После месяца: постепенно увеличивайте

2. **Диверсификация**
   - Не держите все средства в одном боте
   - Используйте разные торговые пары
   - Используйте разные стратегии

3. **Stop-loss**
   - ВСЕГДА устанавливайте stop_loss_percentage
   - Рекомендуется: 10-20% для Grid, 20-30% для DCA
   - Никогда не отключайте stop-loss

4. **Emergency reserve**
   - Держите 20-30% капитала вне бота
   - Для экстренных ситуаций
   - Для быстрой реакции на возможности

5. **Регулярный вывод прибыли**
   - Выводите часть прибыли еженедельно/ежемесячно
   - Не реинвестируйте всю прибыль сразу
   - Зафиксируйте прибыль в стейблкоины

---

## Дальнейшие шаги

### После успешного testnet тестирования

1. **Анализ результатов testnet**
   - Соберите статистику за 3-7 дней
   - Проанализируйте все сделки
   - Проверьте правильность работы стратегий
   - Убедитесь в отсутствии ошибок в логах

2. **Переход на production**
   ```yaml
   # В configs/production.yaml измените:
   sandbox: false  # Переключение на real trading
   dry_run: true   # Сначала проверьте с dry_run!
   ```

3. **Тестирование с dry_run на production**
   - Запустите с dry_run: true на production API
   - Проверьте, что расчеты корректны
   - Убедитесь, что нет ошибок
   - Минимум 24 часа в dry_run режиме

4. **Первый real запуск**
   ```yaml
   sandbox: false
   dry_run: false  # РЕАЛЬНАЯ торговля!
   ```
   - Начните с минимального капитала ($50-100)
   - Следите очень внимательно первые 48 часов
   - Будьте готовы к экстренной остановке

### Оптимизация и масштабирование

1. **Оптимизация параметров**
   - Анализируйте статистику через /pnl
   - Корректируйте grid_levels, amount_per_grid
   - Экспериментируйте с profit_per_grid
   - Тестируйте изменения сначала на testnet

2. **Добавление новых ботов**
   - После успешной работы первого бота
   - Добавьте другие торговые пары
   - Протестируйте DCA стратегию
   - Попробуйте Hybrid режим

3. **Масштабирование капитала**
   - Постепенно увеличивайте капитал (не более чем на 50% в неделю)
   - Следите за drawdown
   - Сохраняйте консервативные параметры risk management

4. **Дополнительные возможности**
   - Настройте Grafana для продвинутой аналитики
   - Интегрируйте дополнительные индикаторы
   - Исследуйте другие биржи и пары
   - Рассмотрите запуск нескольких инстансов бота

### Обучение и развитие

1. **Изучите логи и метрики**
   - Понимайте, почему бот принимает решения
   - Анализируйте успешные и неуспешные сделки
   - Ведите дневник наблюдений

2. **Изучите рынок**
   - Понимайте, в каких условиях Grid работает лучше
   - Когда использовать DCA
   - Как влияет волатильность на результаты

3. **Участвуйте в сообществе**
   - GitHub Discussions
   - Issues и Pull Requests
   - Делитесь опытом и результатами

---

## Заключение

Поздравляем! Вы успешно развернули TRADERAGENT DCA-Grid Bot.

**Помните**:
- ⚠️ Начинайте с testnet
- ⚠️ Используйте малый капитал на старте
- ⚠️ Следите за ботом ежедневно
- ⚠️ Никогда не инвестируйте больше, чем можете потерять
- ⚠️ Это автоматизация, но не "автопилот" - требуется ваш контроль

**Торговля криптовалютами связана с высокими рисками. Это руководство предназначено только для образовательных целей.**

---

## Полезные ссылки

- **Основной репозиторий**: https://github.com/alekseymavai/TRADERAGENT
- **Документация**: См. README.md, CONFIGURATION.md, FAQ.md
- **Issues**: https://github.com/alekseymavai/TRADERAGENT/issues
- **Discussions**: https://github.com/alekseymavai/TRADERAGENT/discussions

---

## Поддержка

Если у вас возникли проблемы:
1. Проверьте раздел "Решение проблем" в этом документе
2. Прочитайте FAQ.md и TROUBLESHOOTING.md
3. Создайте Issue на GitHub с подробным описанием проблемы и логами

---

**Удачи в торговле! 🚀📈**
