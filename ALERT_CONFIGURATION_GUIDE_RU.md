# Руководство по настройке алертов ALMIR Fibonacci Strategy

## 📋 Содержание

1. [Введение](#введение)
2. [Типы алертов](#типы-алертов)
3. [Настройка алерта для экспорта данных](#настройка-алерта-для-экспорта-данных)
4. [Настройка торговых алертов](#настройка-торговых-алертов)
5. [Формат экспортируемых данных](#формат-экспортируемых-данных)
6. [Автоматизация сбора данных](#автоматизация-сбора-данных)
7. [Примеры использования](#примеры-использования)
8. [Решение проблем](#решение-проблем)

---

## Введение

Улучшенная версия ALMIR Fibonacci Strategy поддерживает систему алертов TradingView для экспорта результатов тестирования и уведомлений о торговых сигналах. Это руководство поможет вам правильно настроить алерты для сбора данных и автоматизации торговли.

### Зачем нужны алерты?

- **Экспорт результатов**: Автоматический сбор метрик производительности в CSV-совместимом формате
- **Массовое тестирование**: Быстрое тестирование на множестве пар и таймфреймов
- **Торговые уведомления**: Получение уведомлений о новых сигналах
- **Автоматизация**: Интеграция с ботами и webhook для автоматической торговли

---

## Типы алертов

Стратегия поддерживает следующие типы алертов:

### 1. Export Results (Экспорт результатов)
**Назначение**: Экспорт всех метрик производительности и параметров стратегии

**Когда срабатывает**:
- В конце тестового периода (опция `Export Only at Test End`)
- На каждом баре (если опция отключена)

**Формат сообщения**:
```
EXPORT,<Symbol>,<Timeframe>,<Parameters JSON>,<Metrics>
```

### 2. ALMIR Bullish Signal (Бычий сигнал)
**Назначение**: Уведомление о бычьем сигнале на вход

**Когда срабатывает**: При генерации сигнала на покупку

**Формат сообщения**:
```
ALMIR Strategy: Bullish signal! Score: <confluence_score>
```

### 3. ALMIR Bearish Signal (Медвежий сигнал)
**Назначение**: Уведомление о медвежьем сигнале на вход

**Когда срабатывает**: При генерации сигнала на продажу

**Формат сообщения**:
```
ALMIR Strategy: Bearish signal! Score: <confluence_score>
```

### 4. Long Position Opened (Открыта длинная позиция)
**Назначение**: Уведомление об открытии длинной позиции

### 5. Short Position Opened (Открыта короткая позиция)
**Назначение**: Уведомление об открытии короткой позиции

### 6. Position Closed (Позиция закрыта)
**Назначение**: Уведомление о закрытии позиции

---

## Настройка алерта для экспорта данных

### Шаг 1: Включите экспорт в настройках стратегии

1. Откройте стратегию на графике TradingView
2. Перейдите в настройки стратегии
3. Найдите группу "📤 Export Settings"
4. Включите опцию "Enable Results Export"
5. Рекомендуется включить "Export Only at Test End" для экспорта один раз в конце теста

![Export Settings](https://via.placeholder.com/800x200?text=Export+Settings+Screenshot)

### Шаг 2: Создайте алерт

1. Нажмите на кнопку **"Создать алерт"** (Create Alert) в верхней панели TradingView
2. В поле **"Условие"** выберите:
   - **Индикатор**: "ALMIR Fibonacci Strategy - Enhanced"
   - **Условие**: "Export Results"
3. В поле **"Сообщение"** оставьте значение по умолчанию: `{{alert_message}}`
4. Настройте параметры срабатывания:
   - **Опции**: "Один раз на закрытии бара" (Once Per Bar Close)
   - **Срок действия**: Установите нужный срок или "Бессрочно"
5. Настройте способ уведомления:
   - Email (для ручной обработки)
   - Webhook (для автоматической обработки)
6. Нажмите **"Создать"**

### Шаг 3: Настройка Webhook (опционально)

Для автоматического сбора данных можно использовать webhook:

1. В поле **"Webhook URL"** укажите адрес вашего сервера
2. Пример URL: `https://your-server.com/api/collect-results`
3. Сервер должен принимать POST запросы с телом сообщения в формате:
```
EXPORT,BTCUSDT,1h,{"preset":"Default",...},45,62.22,1.85,-450.50,1250.75,28,17
```

---

## Настройка торговых алертов

### Алерт на бычий сигнал

**Рекомендуется для**: Получения уведомлений о новых возможностях покупки

**Настройка**:
1. Создайте алерт
2. Выберите условие: "ALMIR Bullish Signal"
3. Сообщение по умолчанию: `{{alert_message}}`
4. Опции: "Один раз на закрытии бара"

**Для автоматической торговли**:
```json
{
  "action": "buy",
  "symbol": "{{ticker}}",
  "price": {{close}},
  "confluenceScore": {{plot_0}},
  "timeframe": "{{interval}}"
}
```

### Алерт на медвежий сигнал

**Настройка аналогична бычьему сигналу**, но:
- Условие: "ALMIR Bearish Signal"
- Action: "sell"

### Алерты на открытие/закрытие позиций

**Рекомендуется для**: Отслеживания исполнения стратегии

Настройка аналогична, выберите соответствующее условие:
- "Long Position Opened"
- "Short Position Opened"
- "Position Closed"

---

## Формат экспортируемых данных

### Структура CSV

Алерт "Export Results" формирует данные в CSV-совместимом формате:

```csv
Type,Symbol,Timeframe,Parameters,TotalTrades,WinRate,ProfitFactor,MaxDrawdown,NetProfit,GreenCandleSignals,RedCandleSignals
EXPORT,BTCUSDT,1h,"{""preset"":""Default"",""rsiLength"":10,...}",45,62.22,1.85,-450.50,1250.75,28,17
```

### Поля данных

| Поле | Описание | Пример |
|------|----------|--------|
| **Type** | Тип сообщения | EXPORT |
| **Symbol** | Торговая пара | BTCUSDT |
| **Timeframe** | Таймфрейм | 1h, 4h, 1D |
| **Parameters** | JSON с параметрами стратегии | {"preset":"Default",...} |
| **TotalTrades** | Количество закрытых сделок | 45 |
| **WinRate** | Win Rate в % | 62.22 |
| **ProfitFactor** | Profit Factor | 1.85 |
| **MaxDrawdown** | Максимальная просадка | -450.50 |
| **NetProfit** | Чистая прибыль | 1250.75 |
| **GreenCandleSignals** | Сигналов на зелёных свечах | 28 |
| **RedCandleSignals** | Сигналов на красных свечах | 17 |

### Пример Parameters JSON

```json
{
  "preset": "Default",
  "rsiLength": 10,
  "rsiOversold": 30,
  "rsiOverbought": 65,
  "macdFast": 2,
  "macdSlow": 30,
  "macdSignal": 10,
  "stochLength": 14,
  "stochOversold": 30,
  "stochOverbought": 83,
  "minConfluenceScore": 6,
  "minBarsBetweenSignals": 12,
  "fibLookback": 100
}
```

---

## Автоматизация сбора данных

### Вариант 1: Email → Таблица (Простой)

**Подходит для**: Ручной обработки небольшого количества тестов

1. Настройте алерт с уведомлением на Email
2. После завершения теста получите письмо
3. Скопируйте данные из письма
4. Вставьте в Google Sheets или Excel
5. Используйте функцию "Split text to columns" с разделителем ","

### Вариант 2: Webhook → База данных (Продвинутый)

**Подходит для**: Массового тестирования множества пар

**Необходимые компоненты**:
- Веб-сервер с поддержкой webhook (Node.js, Python Flask, и т.д.)
- База данных (MongoDB, PostgreSQL, CSV файл)
- Скрипт обработки данных

**Пример сервера на Node.js**:

```javascript
const express = require('express');
const fs = require('fs');
const app = express();

app.use(express.text());

app.post('/api/collect-results', (req, res) => {
  const data = req.body;

  // Проверка формата
  if (data.startsWith('EXPORT,')) {
    // Добавление в CSV файл
    fs.appendFileSync('results.csv', data + '\n');
    console.log('Result saved:', data);
  }

  res.status(200).send('OK');
});

app.listen(3000, () => {
  console.log('Webhook server running on port 3000');
});
```

**Пример сервера на Python (Flask)**:

```python
from flask import Flask, request
import csv

app = Flask(__name__)

@app.route('/api/collect-results', methods=['POST'])
def collect_results():
    data = request.get_data(as_text=True)

    if data.startswith('EXPORT,'):
        # Сохранение в CSV
        with open('results.csv', 'a', newline='') as f:
            f.write(data + '\n')
        print(f'Result saved: {data}')

    return 'OK', 200

if __name__ == '__main__':
    app.run(port=3000)
```

### Вариант 3: Telegram Bot (Удобный)

**Подходит для**: Получения уведомлений в мессенджере

1. Создайте Telegram бота через @BotFather
2. Настройте webhook для отправки в Telegram API
3. Используйте сервис-посредник (например, Zapier или Make.com)

**Пример webhook URL для Telegram**:
```
https://api.telegram.org/bot<YOUR_BOT_TOKEN>/sendMessage?chat_id=<YOUR_CHAT_ID>&text={{alert_message}}
```

---

## Примеры использования

### Пример 1: Массовое тестирование на разных таймфреймах

**Цель**: Протестировать стратегию на BTCUSDT на таймфреймах 15m, 1h, 4h, 1D

**Процесс**:
1. Настройте webhook для автоматического сбора
2. Для каждого таймфрейма:
   - Откройте график BTCUSDT
   - Примените стратегию с пресетом "Default"
   - Создайте алерт "Export Results"
   - Установите период тестирования (например, последние 6 месяцев)
3. Дождитесь окончания периода или промотайте историю
4. Соберите все результаты из CSV файла
5. Сравните метрики в Excel/Google Sheets

### Пример 2: Оптимизация параметров

**Цель**: Найти оптимальный `minConfluenceScore` для ETHUSDT 1h

**Процесс**:
1. Создайте таблицу для результатов
2. Для каждого значения (4, 5, 6, 7, 8, 9, 10):
   - Измените `minConfluenceScore` в настройках
   - Создайте алерт с уникальным именем
   - Запустите тест на историческом периоде
3. Соберите результаты
4. Сравните Win Rate, Profit Factor, Max Drawdown
5. Выберите оптимальное значение

### Пример 3: Тестирование пресетов

**Цель**: Сравнить все пресеты на одной паре

**Процесс**:
1. Выберите пару (например, SOLUSDT 4h)
2. Для каждого пресета (Default, Trending, Sideways, Volatile, Conservative, Aggressive):
   - Установите пресет
   - Создайте алерт "Export Results"
   - Название: "SOLUSDT-4h-<Preset>"
3. Соберите все результаты
4. Определите лучший пресет для этой пары

---

## Решение проблем

### Проблема: Алерт не срабатывает

**Возможные причины**:
1. Опция "Enable Results Export" отключена
2. Не достигнут конец тестового периода (если включен "Export Only at Test End")
3. Стратегия не активна на графике

**Решение**:
- Проверьте настройки экспорта
- Убедитесь, что стратегия загружена
- Попробуйте отключить "Export Only at Test End" для теста

### Проблема: Неправильный формат данных

**Возможная причина**: Специальные символы в названии пары

**Решение**:
- Используйте только стандартные пары
- Проверьте кодировку при сохранении в CSV

### Проблема: Webhook не получает данные

**Возможные причины**:
1. Неправильный URL webhook
2. Сервер не запущен
3. Firewall блокирует входящие запросы
4. HTTPS требуется, а используется HTTP

**Решение**:
- Проверьте доступность сервера (ping, curl)
- Используйте сервисы тестирования webhook (webhook.site)
- Настройте HTTPS с помощью Let's Encrypt
- Проверьте логи сервера

### Проблема: Дублирующиеся алерты

**Возможная причина**: Создано несколько алертов на одно условие

**Решение**:
- Удалите лишние алерты
- Используйте уникальные имена для разных тестов

### Проблема: Алерты приходят слишком часто

**Возможная причина**: Отключена опция "Export Only at Test End"

**Решение**:
- Включите опцию для экспорта только в конце
- Или используйте опцию "Один раз на закрытии бара"

---

## Дополнительные ресурсы

### Полезные ссылки

- [TradingView Alert Documentation](https://www.tradingview.com/support/solutions/43000529348-about-tradingview-alerts/)
- [Webhook Testing Tool](https://webhook.site)
- [JSON Validator](https://jsonlint.com/)
- [CSV to JSON Converter](https://www.convertcsv.com/csv-to-json.htm)

### Шаблоны

**CSV заголовок для обработки**:
```csv
Type,Symbol,Timeframe,Parameters,TotalTrades,WinRate,ProfitFactor,MaxDrawdown,NetProfit,GreenCandleSignals,RedCandleSignals
```

**Google Sheets формула для разбора JSON параметров**:
```
=IMPORTJSON(CELL_WITH_JSON)
```
(требуется установка дополнения IMPORTJSON)

---

## Обратная связь

Если у вас возникли вопросы или предложения по улучшению системы алертов:

- Создайте Issue в репозитории
- Опишите вашу ситуацию
- Приложите скриншоты настроек (если возможно)

---

**Версия руководства**: 1.0
**Последнее обновление**: 2026-02-03
**Связанная задача**: #87
**Версия стратегии**: Enhanced
